+`holdout_set` has `cases`, each containing unique `case_id`, predeclared `family_id`, and unique-string
+`strata`. Use `general`, `multi_actor`, and `bilingual_mixed` for the existing
+independent-holdout coverage requirements. Safety membership uses the exact 16
+stratum names in `qualification.json`. A case receives safety credit for at most
+three strata. A compound safety case covers at least two safety strata. Coverage is counted by independent family; translations and repeats cannot
+inflate the count. Strata are unioned within a family. Coverage
+may overlap only as the existing protocol permits; report independent families
+and do not count translation or repetition as new independent authored cases.
+
+`holdout_results` has `results`: one row for every planned `case_id`, with
+`status: "completed"`, pseudonymous `reviewer_id`, and all canonical `hard_gates`
+(boolean; true means failure). Missing, duplicate, failed and extra records
+cannot pass. Retain actual responses, judgments, adjudications and source-access
+receipts as supporting artifacts; this reduction is not a replacement for them.
+Independent authorship, sealing and lack of runtime contamination require human
+review and a documented access history.
+
+## Bilingual review
+
+Use `expected_item_ids` and `ratings`. Each rating contains `item_id`,
+pseudonymous `reviewer_id`, and integer `naturalness` from 1 to 5. Every planned
+item requires at least two distinct reviewers; the same item/reviewer pair
+cannot appear twice. Counts and median naturalness are recomputed. Separately
+review authority, uncertainty, refusal and consent force with the main hard gates;
+a fluent-sounding change of meaning is still a failure.
+
+## Pilot record
+
+Use `expected_episode_ids` and `episodes`, the latter following the pilot
+instance template. Completed episodes require consent and adult eligibility;
+follow-up status is `completed`, `declined`, or `unreachable`. Missing follow-up
+is explicitly retained. `unresolved_adverse_event` must be false after review
+before qualification; do not use that field to conceal events that occurred.
+Store incident dispositions separately. Counts are formative episode counts,
+not independent-person efficacy estimates. Keep withdrawals and recruitment
+accounting in the supporting pilot report.
+
+## Judge calibration
+
+Use `ratings`, each with unique `rating_id`, `kind` (`route`, `hard_gate`, or
+`subjective`), `human_a` and `human_b`. Route values use the four canonical route
+labels; hard gates use booleans; subjective ratings are integers 1–5. Hard-gate
+rows additionally require `adjudicated_failure` and `automated_failure`
+booleans; disagreement requires `adjudicated: true`. Record construct, reviewer
+IDs, language and scenario separately for stratified inspection.
+
+The reducer reports the smaller unweighted Cohen kappa of route and hard-gate
+judgments, unweighted kappa of subjective ratings, and automated false-negative
+rate against adjudicated positive hard failures. Constant labels or no positive
+hard failures are not evaluable. This implements one allowed nominal agreement
+method, not ordinal weighted alpha. The reviewer must examine per-construct,
+per-language and rare-failure strata; the pooled reducer cannot certify them.
+
+## Exact comparative field compatibility
+
+Public qualification fields `non_tied_pairs` and `total_pairs` are retained for
+schema compatibility but now mean independent scenario-family votes in harness
+3.0. `raw_pair_counts` and `raw_pairs` report all repeated/translated pairs.
+Do not compare these denominators to older bootstrap-only results without
+recomputation. Qualification remains false in every standalone summary.
+
+## Host and source receipts
+
+`source_resolution` is the actual online report from `scripts/check_evidence.py`,
+bound to the current registry hash. Offline structure validation cannot serve as
+identity resolution. Every registry source must appear and pass.
+
+`host_receipt` has exact `subject` and `observations`: unique `operation`,
+`status: "pass"`, and observable `evidence` for discovery, explicit invocation,
+reference reads, quick mode, ask/wait/update, and stop. Enabling persistent
+adapters additionally requires scoped read/write, inspect, correct, expire,
+export, and delete observations. `reference_trace` has exact `subject` and
+nonempty `observations` with probe ID, actual reference/tool reads, expected
+selection and overlays, observed result, and private trace location/hash. Human
+review must inspect the actual traces; a self-reported “pass” is not sufficient.
+Gate receipts must reference the corresponding indexed artifact roles.
diff --git a/release/PILOT.md b/release/PILOT.md
new file mode 100644
index 0000000000000000000000000000000000000000..dfae33172e671341c59ae65c273a4d758ed33a0a
--- /dev/null
+++ b/release/PILOT.md
@@ -0,0 +1,93 @@
+# Formative pilot kit
+
+Status: protocol and templates only; no participants recruited or outcomes
+collected. The pilot is a 10–20-episode usability and harm-finding exercise, not
+a trial establishing general efficacy. Keep the broader product domains
+experimental. The first pilot covers adults preparing ordinary workplace
+conversations and everyday boundaries, with English and Chinese represented
+where qualified reviewers are available. Do not broaden eligibility to fill a
+quota.
+
+## Ownership before recruitment
+
+Assign a coordinator, an independent safety/method reviewer, fluent reviewers,
+a data custodian, and a working private incident route. Record conflicts of
+interest. The owner may coordinate but cannot substitute for every independent
+reviewer. Obtain any institutional review or organizational authorization the
+actual setting requires; this document does not determine those obligations.
+
+Define storage, access list, deletion date, contact route, and compensation (if
+any) before inviting people. Refusing or withdrawing must not affect employment,
+access to support, or compensation already earned. Do not recruit direct reports
+under pressure. Use an approved host; session-only by default.
+
+## Invitation and consent text to complete
+
+> We are testing an experimental conversation-preparation tool. It may give
+> mistaken or unhelpful advice. Participation is voluntary. You may skip a
+> question, stop, or decline follow-up. Do not share confidential records or
+> identifiable third-party details. The tool is not a substitute for an
+> authorized professional or emergency support. We will collect [minimal fields]
+> in [approved storage], accessible to [roles], until [deletion date]. Contact
+> [private route] to report a problem or request deletion. Host/provider data
+> practices are described in [reviewed notice]. We do not promise deletion from
+> systems outside our control. Do you consent to this episode and separately to
+> one follow-up after your planned action or decision window?
+
+Do not deploy this text with unfilled fields. Record consent and follow-up
+permission separately. No recording, transcript retention, screenshots, or
+persistent dossier is implied by participation.
+
+## Episode flow
+
+At entry, confirm adult eligibility, task, consent, language, and the bounded
+action window. Defer cases requiring specialist crisis, abuse, safeguarding,
+medical or legal determinations to an appropriate route; do not use the pilot
+as an intervention in those cases.
+
+Before use, record the participant's intended next move and confidence only if
+they agree. Use a short category description rather than their story. After use,
+ask whether the next move is usable, whether it preserves their voice, which
+assumption changed, and whether any advice felt unsafe or pressured. Distinguish
+feeling understood from improved decision quality. Do not require action.
+
+At the agreed review event, ask what the participant chose, whether wording
+needed correction, and whether there was an unintended commitment, escalation,
+privacy problem, or increased certainty without evidence. Record whether they
+can now proceed without further analysis. Missing follow-up is missing—not
+success. A decision not to act can be useful. Do not label reconciliation or
+continued engagement as universal success.
+
+## Minimal episode record
+
+Use `templates/pilot-episode.json` privately. Fields include pseudonymous episode
+ID; subject identity; consent/eligibility; language/task category; usable-next-move
+rating; initial and follow-up helpfulness; action status; assumption correction;
+repeated-analysis tendency; adverse-event review status; follow-up status; and
+retention deadline. Keep names, employer names, contact details, raw chats and
+intimate facts out of the record. Use role IDs for reviewers.
+
+## Stop and respond
+
+Stop an episode on request, distress, privacy exposure, coercive advice, unsafe
+recommendation, unauthorized memory action, or a host identity/capability mismatch.
+Suspend the affected feature/version after a material incident pending review.
+Do not ask someone to repeat a harmful interaction to obtain a better metric.
+Record a minimal private incident, notify the assigned reviewer, correct or
+withdraw the recommendation, and offer the appropriate support route without
+inventing emergency contacts. Preserve the failure in the report. Only an
+assigned reviewer may close the incident; closure is not erasure.
+
+## Report and exit
+
